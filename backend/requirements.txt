# ── Web framework ──────────────────────────────
fastapi>=0.104.1
uvicorn>=0.27.0

# ── Database ───────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9     # Driver kết nối PostgreSQL
geoalchemy2>=0.14.0        # Hỗ trợ kiểu dữ liệu PostGIS (geometry)
alembic>=1.13.0            # Migration DB

# ── Cache ──────────────────────────────────────
redis>=5.0.0

# ── Config / Validation ────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0   # Đọc biến .env vào Settings class

# ── HTTP Client (gọi TomTom, Goong, OpenWeather) ─
httpx>=0.27.0
requests>=2.31.0              # Gọi Overpass API (seed data)

# ── Scheduler (cron jobs, ingest API định kỳ) ──
apscheduler>=3.10.0

# ── Auth ───────────────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT
passlib[bcrypt]>=1.7.4             # Hash password

# ── Data / AI ──────────────────────────────────
pandas>=2.2.0
numpy>=1.26.4
scikit-learn>=1.4.0
joblib>=1.3.0              # Load/save model .pkl

# ── Tiện ích ───────────────────────────────────
Faker==22.5.1              # Sinh mock data (dev only)
packaging>=24.0